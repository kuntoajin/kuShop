const path = require("path");

module.exports = {
    home: [path.resolve(__dirname, "..", "src", "pages", "home", "index.tsx")],
    detail: [path.resolve(__dirname, "..", "src", "pages", "detail", "index.tsx")],
}